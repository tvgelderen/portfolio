import Head from 'next/head'

import Hero from '../components/Hero'
import About from '../components/About'
import Skills from '../components/Skills'
import Projects from '../components/Projects'
import Contact from '../components/Contact'
import ThemeChanger from "../components/ThemeChanger"

export default function Home() {
  return (
    <div className='min-h-screen snap-y snap-mandatory overflow-scroll z-0'>
      <Head>
        <title>Portfolio</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/icons/favicon.ico" />
      </Head>

      <section id='hero' className='snap-center' >
        <Hero />
      </section>

      <section id='about' className='snap-top'>
        <About />
      </section>

      <section id='skills' className='snap-top'>
        <Skills />
      </section>

      <section id='projects' className='snap-top'>
        <Projects />
      </section>

      <section id='contact' className='snap-top'>
        <Contact />
      </section>

      <ThemeChanger />
    </div>
  )
}
